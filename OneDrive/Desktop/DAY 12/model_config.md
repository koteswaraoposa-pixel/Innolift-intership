# Model integration notes

- Flask app loads the trained model from:
  - `final_model.pkl` (path: project root: `c:/Users/HP/OneDrive/Desktop/DAY 12/final_model.pkl`)

- The previously shared name `final_model.pkl` must exist at that exact path for the app to import successfully.

- If your actual file is named differently, update `MODEL_PATH` in `app.py`.

