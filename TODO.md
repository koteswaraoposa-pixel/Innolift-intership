# TODO (LoanGuard AI)

- [ ] Add Google OAuth (Authlib) login + callback routes in app.py
- [ ] Update Users table schema to store Google id (google_sub) (SQLite migration)
- [ ] Update requirements.txt to include Authlib
- [ ] Update templates/login.html to include “Login with Google” button
- [ ] Update templates/base.html to show user state correctly for both local + Google login
- [ ] Improve background/UI and recolor parameter-related UI elements consistently in static/css/style.css
- [ ] Run server and manually test: local login, Google login, protected pages

